import styled from 'styled-components';

export const StyleSpan = styled.p`
  margin: 0px 0px 5px 5px;

  color: var(--error);
  font-weight: 500;
  font-size: 14px;
  line-height: 19px;
  text-align: left;
`;
