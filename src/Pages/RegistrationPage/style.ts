import styled from "styled-components";

export const ButtonRegisterHeader = styled.button`
  background-color: var(--color-green-primary);
  border: none;
  font-size: 20px;
  font-weight: bolder;
  color: var(--color-green-primary-hover);
`;
